schema: 1

apt:
  # Keep false while testing so future apply work cannot remove packages.
  enforce: true
  packages:
    - curl
    - git
    - python3
    - python3-pip
    - thonny
    - figlet

overlay:
  enabled: true

background:
  enabled: true
